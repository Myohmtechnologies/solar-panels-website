import CityLayout from '@/components/layout/CityLayout';

export default function AlpesMaritimesVillesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <CityLayout>{children}</CityLayout>;
}
